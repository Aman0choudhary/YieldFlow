# YieldFlow Scripts

Deployment, testnet setup, demo reset, and later relayer setup scripts will live here.

Planned scripts:

- `demo:reset`: fund test accounts, deploy/configure contracts, seed one employer and one employee.
- `deploy:testnet`: build and deploy Soroban contracts to Stellar testnet.
- `verify:demo`: print deployment IDs, account addresses, and demo URLs.
